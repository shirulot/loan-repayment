# 提前还贷计算器项目约定

## 日常开发

- 默认使用中文回答；代码标识符、专业术语和用户明确要求的内容可使用英文。
- 每次编码、修复或调整前，先读取当前 checkout 的真实源码、目录结构、调用链和已有公共能力；检查 Git 工作区，保留与当前任务无关的用户改动，不根据旧版本代码做假设。
- 当前项目是 Flutter/Dart 项目，优先使用 Dart 3 空安全、`const`、`final`、`required` 和明确的可空类型；不要引入 Kotlin/Java 专属实现，也不要跨仓库直接搬运其他语言的扩展方法。
- 页面负责状态和整体编排；独立且可理解的 UI 区域、表单区块和重复展示单元拆到语义明确的独立文件。仅被一个页面使用的小 Widget 可以保持私有，避免为了减少行数制造碎片组件。
- 默认保持现有业务逻辑、数据模型、交互顺序、生命周期、默认值和错误语义不变；只调整组织方式时，不顺带修改计算公式、异步时序或公共接口。确需改变行为时，说明原因、影响和验证结果。
- 写新代码前先搜索现有的组件、格式化器、服务和 `core/extensions` 扩展；只有现有能力无法满足、语义不匹配或改造风险过高时，才新增实现。不要为了统一写法批量替换不完全等价的调用。
- 扩展方法按类型和职责放在 `lib/core/extensions/<name>_extensions.dart`，保持无副作用、语义单一、调用点可读；只用于纯转换或轻量校验，不隐藏请求、导航、日志、`setState`、Timer 或业务状态。
- 使用扩展替换已有代码时，必须保持原有空值、默认值、异常和空白字符语义；新增扩展补充简短注释和对应单元测试。
- 文案、颜色、尺寸、时长和样式优先复用或集中放置在现有功能级资源/样式文件中，避免在多个 Widget 中散落相同常量或硬编码颜色。
- 注释解释业务规则、跨层边界、生命周期、性能保护和非直观限制，不重复描述语法。
- 其他仓库只能作为只读参考；除非用户明确指定，否则不要修改其他项目的源码或 `AGENTS.md`。

## 页面与状态

- `build` 主要描述页面结构和状态分支；复杂区域拆为职责单一的 Widget 或方法。
- 页面自身创建的 `ScrollController`、`PageController`、Timer 等资源必须在 `dispose` 中释放；异步回调更新 UI 前检查 `mounted`。
- 保持当前项目已有的状态管理方式，不因局部整理强行替换架构或混用多套状态管理方案。

## 验证与提交

- 修改完成后，从项目根目录对受影响文件执行 `dart format`，并执行 `flutter analyze` 和 `flutter test`；若测试有警告或工具失败，区分记录代码问题、工具问题和已有警告。
- 提交前执行 `git diff --check`，核对变更文件和 diff 范围，避免把无关文件、临时文件或跨项目修改带入提交。
- 输出结果时说明修改文件、核心变化、验证命令、验证结果和剩余风险；未修改或无需修改时直接说明。
